services:
  arianna:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5051:5051"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    working_dir: /app
